---
const {title} = Astro.props
---
<section class="featurecategory">
  <h1>{title}</h1><br/>
  <div><slot/></div>
</section>

<style lang="scss">
  .featurecategory{
    display: block;
    margin-inline: auto;
    color: #666;
    position: relative;
    border: none;
    letter-spacing: 0.2px;
    & > div {
      display: flex;
      justify-content: space-around;
      flex-flow: row wrap;
      gap: 3rem;
      border-color: hsl(var(--color1hsl));
      box-shadow: 0 3px 10px -2px #bbb;
      padding: 2.5rem 1.4rem 3rem;
      border-radius: 8px;
      & > :global(div), & > :global(article), & > :global(article){
        flex: 3 1 30rem;
        max-width: 30rem;
        margin-bottom: 3rem;
        margin-inline: auto;
      }
    }
    
  }
</style>