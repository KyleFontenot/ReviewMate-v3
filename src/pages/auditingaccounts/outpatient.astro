---
import FeatureLayout from '../../layouts/FeatureLayout.astro'
import Feature from '../../components/Feature.astro'
import CallToAction from '../../components/CallToAction.astro'
import featureList from "../../featureList.js"
const outpatient = featureList.auditing_modules.find(obj => obj.title === "Outpatient")
---
<FeatureLayout>
<Feature title="Outpatient" imgSrc="/src/images/rodnae.jpg" imgAlt="Doctors meeting together"> 
  {}
    <p>{outpatient?.long}</p>
    <p>The IPPS (Facility Inpatient) auditing module of ReviewMate provides accuracy rates for all elements including:</p>
  <ul slot="bulletpoints">
  {outpatient?.bullets?.map(point => {
    return (
      <li>{point}</li>
    )
  }
  )}
  </ul>
    
</Feature>
<CallToAction></CallToAction>
</FeatureLayout>